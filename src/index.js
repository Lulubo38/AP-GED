import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// Suppression de l'importation de reportWebVitals
// import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// Suppression de l'appel à reportWebVitals
// reportWebVitals();
